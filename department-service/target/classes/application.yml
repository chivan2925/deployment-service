server:
  port: 8082

spring:
  application:
    name: department-service
  cloud:
    config:
      enabled: false  
  datasource:
    url: jdbc:mysql://localhost:3306/department_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  

